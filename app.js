
const START=126,GOAL=70,STEP=2.5,$=s=>document.querySelector(s);
const state=load();function load(){const b={weights:[],waists:[],photos:[],start:START,goal:GOAL,lastCelebrated:START};try{return {...b,...JSON.parse(localStorage.getItem('unburdened')||'{}')}}catch{return b}}function save(){localStorage.setItem('unburdened',JSON.stringify(state))}
function w(){return state.weights.length?state.weights.at(-1).value:state.start}function lost(){return Math.max(0,state.start-w())}function remain(){return Math.max(0,w()-state.goal)}function fmt(n){return Number(n).toFixed(Number(n)%1?1:0)}function today(){return new Date().toISOString().slice(0,10)}
function cps(){let a=[126,125],x=122.5;while(x>=70){a.push(x);x-=2.5}return [...new Set(a)].sort((a,b)=>b-a)}function next(){return cps().filter(x=>x<w()).sort((a,b)=>b-a)[0]??GOAL}function overall(){return Math.min(100,Math.max(0,lost()/(START-GOAL)*100))}
function setView(v){document.querySelectorAll('#bottomNav button').forEach(b=>b.classList.toggle('active',b.dataset.view===v));({home,log,progress,milestones,more}[v]||home)();scrollTo(0,0)}document.querySelectorAll('#bottomNav button').forEach(b=>b.onclick=()=>setView(b.dataset.view));
function travelerSVG(){return `<svg viewBox="0 0 95 140" aria-hidden="true"><g stroke="#102d3c" stroke-width="3" stroke-linejoin="round"><circle cx="51" cy="23" r="17" fill="#c98659"/><path d="M32 21c2-19 35-24 39 0-8-4-14-11-21-5-7-4-11 5-18 5z" fill="#111f2b"/><path d="M28 49c11-12 39-12 49 1l4 52H22z" fill="#1d3342"/><rect x="15" y="51" width="44" height="58" rx="12" fill="#24475c"/><path d="M22 62h29M23 76h27" stroke="#5f889b"/><path d="M26 108l-8 25M58 105l17 24" stroke="#172b36" stroke-width="11" stroke-linecap="round"/><path d="M13 132h18M69 130h18" stroke="#5b4b39" stroke-width="8" stroke-linecap="round"/><path d="M20 54c-8 11-9 29-6 43M58 53c14 10 18 24 19 42" fill="none"/></g></svg>`}
function heroSVG(){return `<svg viewBox="0 0 520 560" preserveAspectRatio="none" aria-hidden="true"><defs><linearGradient id="m" x1="0" y1="0" x2="0" y2="1"><stop stop-color="#91bac1"/><stop offset="1" stop-color="#5f9180"/></linearGradient></defs><circle cx="430" cy="95" r="54" fill="#fff0a0"/><path d="M70 310L240 88l180 222z" fill="#8eb4ba"/><path d="M240 88l-70 92 72-20 55 17z" fill="#f7fbfa"/><path d="M240 88l180 222-150-100z" fill="#6d9d91"/><path d="M0 410Q115 315 245 410T520 395V560H0Z" fill="#80b18b"/><path d="M0 468Q110 388 230 475T520 440V560H0Z" fill="#649f78"/><g fill="#35745f"><path d="M44 350l18-54 18 54z"/><path d="M85 365l17-50 17 50z"/><path d="M410 352l19-58 19 58z"/><path d="M455 374l18-53 18 53z"/></g><path d="M390 128C330 158 335 198 305 223S345 262 296 294s30 63-27 88" fill="none" stroke="#f9e7b1" stroke-width="9" stroke-linecap="round"/></svg>`}
function home(){const cur=w(),n=next();let arr=cps(),ni=arr.indexOf(n),above=[arr[ni-1],arr[ni-2],arr[ni-3]].filter(x=>x!==undefined);let stepProg=cur===n?100:Math.max(0,Math.min(100,((Math.ceil((START-cur)/STEP)*STEP-(cur-n))/STEP)*100));$('#view').innerHTML=`<section><div class="hero">${heroSVG()}<div class="brand"><h1>Unburdened</h1><p>My Journey • Small steps. Big changes.</p></div><div class="hero-goal"><span class="flag">🚩</span><strong>${GOAL} kg</strong><small>THE GOAL</small></div><div class="trail"><div class="stone done s0">✓ 125</div><div class="stone current s1">${fmt(cur)}<small>YOU ARE HERE</small></div><div class="stone next s2">${fmt(n)}</div><div class="stone s3">${fmt(above[0]??Math.max(GOAL,n-STEP))}</div><div class="stone s4">${fmt(above[1]??Math.max(GOAL,n-STEP*2))}</div><div class="stone s5">${fmt(above[2]??Math.max(GOAL,n-STEP*3))}</div></div><div class="traveler">${travelerSVG()}</div></div><div class="home-body"><div class="card mission"><div class="label">Current Mission</div><div class="route">${fmt(cur)} kg → ${fmt(n)} kg</div><div class="progressbar"><i style="width:${stepProg}%"></i></div><div class="split muted" style="font-size:13px;margin-top:8px"><span>${fmt(lost())} kg down</span><span>${fmt(Math.max(0,cur-n))} kg to next step</span></div></div><div class="grid3"><div class="card stat"><strong>${fmt(cur)}</strong><small>Current kg</small></div><div class="card stat"><strong>${fmt(lost())}</strong><small>Total lost</small></div><div class="card stat"><strong>${fmt(remain())}</strong><small>To goal</small></div></div><div class="card soft quote">“Progress, not perfection.”</div></div></section>`}
function log(){$('#view').innerHTML=`<section class="screen"><div class="head"><h1>Log Weight</h1><span class="mini">Weekly</span></div><div class="scale-icon"></div><div class="card form"><label>Date</label><input id="date" type="date" value="${today()}"><label>Weight (kg)</label><input id="weight" type="number" step=".1" inputmode="decimal" placeholder="${fmt(w())}"><div class="helper">✓ Same time, same conditions for the most useful trend.</div><label>Waist (cm) <span class="muted">— optional</span></label><input id="waist" type="number" step=".1" inputmode="decimal" placeholder="e.g. 110"><p class="muted" style="font-size:12px">Measure through the centre of your belly button. About every 2 weeks.</p><label>Notes <span class="muted">— optional</span></label><textarea id="notes" placeholder="How are you feeling? Anything to note?"></textarea><button id="saveLog" class="btn">Save Weight</button></div><div class="notice">💡 You're not just tracking a number; you're building a healthier you.</div></section>`;$('#saveLog').onclick=()=>{let v=parseFloat($('#weight').value),wa=parseFloat($('#waist').value),d=$('#date').value,notes=$('#notes').value.trim();if(!Number.isNaN(v)){state.weights.push({date:d,value:v,notes});checkCelebration(v)}if(!Number.isNaN(wa))state.waists.push({date:d,value:wa});save();setView('home')}}
function progress(){let cur=w();$('#view').innerHTML=`<section class="screen"><div class="head"><h1>Current Progress</h1></div><div class="card center"><div class="big">${fmt(cur)} kg</div><h2>${fmt(lost())} kg down</h2><p class="muted">${fmt(remain())} kg to go</p><div class="progressbar"><i style="width:${overall()}%"></i></div><div class="split muted" style="font-size:12px;margin-top:8px"><span>${START} kg Start</span><span>${GOAL} kg Goal</span></div></div><div class="card soft center"><div class="title">Next checkpoint</div><div class="big" style="font-size:34px">${fmt(next())} kg</div><div class="muted">${fmt(Math.max(0,cur-next()))} kg to go</div></div><div class="grid3"><div class="card stat"><strong>${fmt(lost())}</strong><small>Total lost</small></div><div class="card stat"><strong>${STEP}</strong><small>Step size</small></div><div class="card stat"><strong>${cps().filter(x=>x<cur).length}</strong><small>Steps to go</small></div></div>${chart()}<div class="card soft quote">Individual numbers can go up and down. The trend is what matters.</div></section>`}
function chart(){let vals=state.weights.slice(-12);if(vals.length<2)return `<div class="card"><h2>Weight trend</h2><div class="chart"><div class="chart-empty">Your trend appears after two weigh-ins.<br>One reading never tells the whole story.</div></div></div>`;let min=Math.min(...vals.map(x=>x.value),GOAL),max=Math.max(...vals.map(x=>x.value),START);let pts=vals.map((x,i)=>`${(i/(vals.length-1))*100},${95-((x.value-min)/(max-min||1))*88}`).join(' ');return `<div class="card"><h2>Weight trend</h2><div class="chart"><svg viewBox="0 0 100 100" preserveAspectRatio="none"><polyline points="${pts}" fill="none" stroke="#27a879" stroke-width="2.5" vector-effect="non-scaling-stroke"/></svg></div></div>`}
function milestones(){let cur=w();$('#view').innerHTML=`<section class="screen"><div class="head"><h1>Milestones</h1></div><div class="tabs"><button class="active">All Steps</button><button>Key Milestones</button></div><div class="card timeline">${cps().slice().reverse().map(x=>{let done=cur<=x,cc=next()===x,major=x%10===0||x===120,goal=x===GOAL;return `<div class="mile ${done?'done':''} ${cc?'current':''} ${major?'major':''} ${goal?'goal':''}"><strong>${fmt(x)} kg</strong><small>${x===START?'Starting point':goal?'Goal':done?`${fmt(START-x)} kg lost`:`${fmt(cur-x)} kg to go`}</small></div>`}).join('')}</div></section>`}
function more(){$('#view').innerHTML=`<section class="screen"><div class="head"><h1>More</h1></div><div class="card"><h2>Unburdened</h2><img class="logo-preview" src="assets/unburdened-logo.png" alt="Unburdened logo"><p class="muted">Less weight. More life.</p></div><div class="card"><h2>My Goals</h2><div class="split"><span>Starting weight</span><strong>${state.start.toFixed(1)} kg</strong></div><hr><div class="split"><span>Ultimate goal</span><strong>${state.goal.toFixed(1)} kg</strong></div><hr><div class="split"><span>Checkpoint size</span><strong>${STEP} kg</strong></div></div><div class="card"><h2>Progress Photos</h2><p class="muted">Front • side • back, roughly every 4 weeks.</p><button id="addPhotos" class="btn">Add Photos</button><div id="photoList">${photos()}</div></div><div class="card"><h2>Your data</h2><p class="muted">Saved on this device. Export a backup anytime.</p><div class="backup"><button id="export" class="btn light">Export</button><button id="importBtn" class="btn light">Import</button></div><input id="importFile" type="file" accept="application/json" hidden></div></section>`;$('#addPhotos').onclick=()=>$('#photoPicker').click();$('#export').onclick=exportSave;$('#importBtn').onclick=()=>$('#importFile').click();$('#importFile').onchange=importSave}
function photos(){if(!state.photos.length)return `<p class="muted">No progress photos added yet.</p>`;return state.photos.slice().reverse().map(p=>`<div class="photo-group"><strong>${p.date}</strong><div class="photo-grid">${p.images.map(i=>`<img src="${i}">`).join('')}</div></div>`).join('')}
$('#photoPicker').onchange=async e=>{let files=[...e.target.files].slice(0,3);if(!files.length)return;let images=await Promise.all(files.map(resize));state.photos.push({date:today(),images});save();more()};function resize(f){return new Promise(resolve=>{let r=new FileReader;r.onload=()=>{let im=new Image;im.onload=()=>{let c=document.createElement('canvas'),s=Math.min(1,900/im.width);c.width=im.width*s;c.height=im.height*s;c.getContext('2d').drawImage(im,0,0,c.width,c.height);resolve(c.toDataURL('image/jpeg',.82))};im.src=r.result};r.readAsDataURL(f)})}
function checkCelebration(v){let newly=cps().filter(x=>v<=x&&state.lastCelebrated>x);if(!newly.length)return;let hit=Math.min(...newly);state.lastCelebrated=hit;save();setTimeout(()=>celebrate(hit),80)}function celebrate(hit){let o=$('#celebrate');o.classList.remove('hidden');o.innerHTML=`<div class="celebrate-box"><div class="confetti">◆ ✦ ● ✦ ◆</div><h1>Checkpoint Reached!</h1><div style="font-size:90px">🚩</div><div class="kg">${fmt(hit)} kg</div><h2>${fmt(START-hit)} kg down</h2><p class="muted">${fmt(hit-GOAL)} kg to go</p><div class="card soft">Another step unlocked. Keep going.</div><button class="btn" onclick="document.getElementById('celebrate').classList.add('hidden')">Continue</button></div>`}
function exportSave(){let blob=new Blob([JSON.stringify(state,null,2)],{type:'application/json'}),a=document.createElement('a');a.href=URL.createObjectURL(blob);a.download=`unburdened-save-${today()}.json`;a.click();URL.revokeObjectURL(a.href)}function importSave(e){let f=e.target.files[0];if(!f)return;let r=new FileReader;r.onload=()=>{try{Object.assign(state,JSON.parse(r.result));save();more()}catch{alert('That save file could not be read.')}};r.readAsText(f)}
home();if('serviceWorker'in navigator)navigator.serviceWorker.register('sw.js');
